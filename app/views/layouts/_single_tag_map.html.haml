= text_field_tag(prefix + '_input') if text_field
%button{:id => prefix + '_show_button'} Show
%table
  %tr
    %td
      %div.hidden.map{:id => prefix + '_map'}
        &nbsp;
    %td
      %div{:id => prefix + '_mi'}